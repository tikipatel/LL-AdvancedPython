"""
Python Comprehensions

Can be applied to lists, sets, and dictionaries

recall we used map -> takes a function and applies that to each value
e.g. f_temps = list(map(FtoC, [1,2,3]))

The above is same as:
f_temp = [(t*9/5)+32 for t in [1,2,3]]
"""

